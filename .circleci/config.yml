version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:latest
    steps:
      - checkout
      - run:
          name: Dependencies
          command: sudo apt-get update -y && sudo apt-get install -y bc schedtool
      - run:
          name: Build
          command: ./build.sh